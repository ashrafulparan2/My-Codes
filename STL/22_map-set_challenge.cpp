#include <bits/stdc++.h>
using namespace std;

int32_t main()
{
    map<string, int> m;

    string line,word;
    ifstream in_file {"word.txt"};

    if(in_file)
    {
        while(getline(in_file,line))
        {
            stringstream ss(line);
        }
    }

    return 0;
}
